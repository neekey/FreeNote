/**
 * configuration
 */

(function(){


var app = {

	/* 所有的backbone model */
	models: {},
	/* 所有的backbone collection */
	collections: {},
	/* 配置项 */
	config: {
	},
	/* 所有的模块 */
	mods: {},
    /* 所有的view */
    views: {}
};

window[ 'freenote' ] = window[ 'APP' ] = app;
})();
